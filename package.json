{
  "name": "eraser-personalized",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "client",
    "shared",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
